<p id="notice"><%= notice %></p>
<br>
<div class="nav justify-content-center">  
  <h1>Status</h1>
</div>

<div class="nav justify-content-center"> 
  <table class="table table-hover">
      <thead>
        <tr>
          <th>Nome</th>
          <th colspan="3"></th>
        </tr>
      </thead>

    <tbody>
      <% @progresses.each do |progress| %>
        <tr>
          <td><%= progress.name %></td>
          <td><%= link_to 'Visualizar', progress %></td>
          <td><%= link_to 'Editar', edit_progress_path(progress) %></td>
          <td><%= link_to 'Excluir', progress, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<br>

<div class="nav justify-content-center">
  <%= link_to 'Novo Status', new_progress_path, class:"btn btn-light"%>
</div>
